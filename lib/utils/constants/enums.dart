enum TextSizes {
  small,
  medium,
  large,
}

enum OrderStatus {
  Processing,
  Shipped,
  Delivered,
  Pending,
}

enum PaymentMethod {
  payPal,
  googlePay,
  applePay,
  creditCard,
  masterCard,
  visaCard,
  paytm,
}

enum ProductType {
  single,
  variable,
}
